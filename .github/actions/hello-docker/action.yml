name: Hello World
author: Oliver Pike
description: Some description
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true
    default: Oliver
outputs:
  time:
    description: "The greeting time"
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.who-to-greet }}
